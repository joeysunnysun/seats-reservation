# server
server:
  port: 8080

spring:
  #  db
  datasource:
    url: jdbc:mysql://localhost:3306/esun?serverTimezone=Asia/Taipei
    username: ${DB_USER_NAME}
    password: ${DB_USER_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate.ddl-auto: update
    #    show-sql: true
    properties:
      hibernate:
        format_sql: true
